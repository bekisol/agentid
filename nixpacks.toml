[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate && pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "uvicorn registry.server:app --host 0.0.0.0 --port $PORT"
